/**
 * EmailSignup 컴포넌트에서 Inputform으로 넘겨줄때 ValueList의 Type
 */
interface ValueListType {
    name: string;
    value: string;
    placeholder: string;
    valid: boolean
}

export default ValueListType;