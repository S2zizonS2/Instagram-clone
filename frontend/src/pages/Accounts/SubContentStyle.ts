import { css } from "@emotion/react";

/**
 * /accounts 페이지 main content의 하위 컨텐츠의 스타일
 */
const SubContentStyle = css`
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 0.95rem;
    min-height: 4.5rem;

    a {
        color: #47afff;
        cursor: pointer;
    }
`;

export default SubContentStyle;