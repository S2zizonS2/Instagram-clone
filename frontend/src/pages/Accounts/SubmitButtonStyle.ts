import { css } from "@emotion/react";


/**
 * /accounts 에서 사용되는 SubmitButtonStyle
 */
const SubmitButtonStyle = css`
    button[type="submit"] {
            background-color: #47afff;
            color: white;
            font-weight: 600;
            text-align: center;
            font-size: 0.9rem;
            margin-top: 0.8rem;
    }

`;
export default SubmitButtonStyle;